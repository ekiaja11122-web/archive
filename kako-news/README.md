# کاکو نیوز — سامانهٔ خبرخوان و انتشار خودکار

سامانه‌ای که به‌صورت دوره‌ای از چند منبع خبری، اخبار مرتبط با **شیراز** را جمع‌آوری
می‌کند، آن‌ها را با رعایت اصول نگارش خبری فارسی بازنویسی می‌کند، در یک **صف تأیید**
می‌گذارد تا سردبیر ببیند، و پس از تأیید در **سایت وردپرسی کاکو نیوز** و **کانال تلگرام**
منتشر می‌کند.

> وضعیت: **کامل** — هر ۸ مایل‌استون انجام شده و سامانه آمادهٔ
> راه‌اندازی روی سرور است.
> مراحل بعدی در بخش [نقشهٔ راه](#نقشهٔ-راه) فهرست شده‌اند.

---

## مسیر یک خبر در سامانه

```
منابع خبری
    │
    ▼
[۱] جمع‌آوری (Collector)        →  جدول raw_articles
    │                              آداپتور RSS یا اسکرِیپر اختصاصی
    ▼
[۲] فیلتر مرتبط‌بودن با شیراز   →  خبرهای نامرتبط: وضعیت irrelevant (حذف نمی‌شوند)
    │
    ▼
[۳] تشخیص تکراری (Dedup)        →  منبع دوم به‌عنوان «منبع تکمیلی» لینک می‌شود
    │
    ▼
[۴] بازنویسی با هوش مصنوعی      →  جدول articles با وضعیت pending_review
    │                              (متن منبع هرگز عیناً کپی نمی‌شود)
    ▼
[۵] صف تأیید (پنل مدیریت)       →  سردبیر ویرایش/تأیید/رد می‌کند
    │
    ▼
[۶] انتشار                      →  وردپرس  +  کانال تلگرام  (جدول publications)
```

---

## استک فنی

| لایه | انتخاب |
|---|---|
| زبان و رانتایم | Node.js 22 + TypeScript (بدون مرحلهٔ build در توسعه) |
| دیتابیس | PostgreSQL 16 |
| سایت | **وردپرس** (سایت فعلی کاکو نیوز) — انتشار از راه REST API |
| پنل تأیید | سرویس وب سبک داخل همین پروژه |
| بازنویسی | OpenAI API (مدل از فایل کانفیگ قابل تغییر) |
| تلگرام | Telegram Bot API |

**چرا Next.js نساختیم؟** سایت کاکو نیوز وردپرسی است و صفحهٔ اصلی، دسته‌بندی،
صفحهٔ خبر و نشانی‌های سئوفرندلی فارسی را خودش دارد. ساختن یک فرانت‌اند دوم یعنی
نگهداری و سئوی موازی. پس ماژول انتشار، پست را مستقیم در وردپرس می‌سازد. اگر روزی
خواستید سایت را از وردپرس جدا کنید، همین سرویس یک API خواندنی هم می‌دهد که
هر فرانت‌اندی بتواند رویش سوار شود.

---

## پیش‌نیازها

- **Node.js نسخهٔ ۲۲ یا بالاتر** — از [nodejs.org](https://nodejs.org) نسخهٔ LTS
- **PostgreSQL نسخهٔ ۱۶ یا بالاتر** — یا با Docker (پایین‌تر توضیح داده شده)

---

## نصب و راه‌اندازی

### گام ۱ — نصب وابستگی‌ها

```bash
cd kako-news
npm install
```

### گام ۲ — بالا آوردن دیتابیس

اگر PostgreSQL روی سیستم‌تان نصب است، یک دیتابیس بسازید:

```bash
createdb kako_news
```

یا با Docker (ساده‌تر):

```bash
docker compose up -d
```

### گام ۳ — ساخت فایل تنظیمات

```bash
cp .env.example .env
```

سپس `.env` را باز کنید و مقادیر را پر کنید:

| متغیر | توضیح | لازم از مایل‌استون |
|---|---|---|
| `DATABASE_URL` | نشانی اتصال به PostgreSQL | ۱ |
| `OPENAI_API_KEY` | کلید از [platform.openai.com](https://platform.openai.com/api-keys) | ۴ |
| `OPENAI_MODEL` | مدل بازنویسی، مثلاً `gpt-4o-mini` | ۴ |
| `ADMIN_USERNAME` / `ADMIN_PASSWORD` | ورود به پنل تأیید | ۵ |
| `SESSION_SECRET` | کلید امضای کوکی نشست | ۵ |
| `WORDPRESS_URL` | نشانی سایت، مثلاً `https://kakonews.ir` | ۶ |
| `WORDPRESS_USERNAME` | نام کاربری وردپرس | ۶ |
| `WORDPRESS_APP_PASSWORD` | «رمز برنامه» وردپرس (نه رمز اصلی!) | ۶ |
| `TELEGRAM_BOT_TOKEN` | توکن ربات از [@BotFather](https://t.me/BotFather) | ۷ |
| `TELEGRAM_CHANNEL_ID` | شناسهٔ کانال، مثل `@kakonews` | ۷ |

برای ساخت مقدار تصادفی `SESSION_SECRET`:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> **فایل `.env` هرگز نباید در گیت کامیت شود.** در `.gitignore` قرار دارد.

#### ساخت «رمز برنامه» وردپرس

پیشخوان وردپرس ← **کاربران** ← پروفایل کاربر ← بخش **Application Passwords** ←
یک نام مثل `kako-bot` بدهید ← رمز تولیدشده را در `WORDPRESS_APP_PASSWORD` بگذارید.
این رمز فقط برای API است و با رمز ورود شما فرق دارد.

### گام ۴ — ساخت جدول‌های دیتابیس

```bash
npm run migrate
```

### گام ۵ — بررسی سلامت تنظیمات

```bash
npm run doctor         # بررسی کامل: تنظیمات، دیتابیس، منابع، سرویس‌ها
```

### گام ۶ — اجرای نمایشی (بدون نیاز به هیچ کلید API)

```bash
npm run demo
```

کل مسیر خبر را با دادهٔ نمونه اجرا می‌کند: جمع‌آوری، فیلتر شیراز، تشخیص
تکراری و ساختن خبر در صف تأیید. سپس:

```bash
npm run serve
```

و در مرورگر **http://127.0.0.1:7799** پنل را ببینید.

> متن خبرهای این حالت **نمونه‌اند، نه بازنویسی واقعی** — با مدل
> `demo-offline` علامت خورده‌اند. برای بازنویسی واقعی، کلید OpenAI را در
> `.env` بگذارید و `npm run rewrite` را اجرا کنید.

---

## فرمان‌های موجود

| فرمان | کار |
|---|---|
| `npm run migrate` | اعمال مهاجرت‌های دیتابیس |
| `npm run db:status` | نمایش وضعیت اتصال و فهرست مهاجرت‌ها |
| `npm run demo` | اجرای نمایشی کل مسیر با دادهٔ نمونه، بدون کلید API |
| `npm run config:check` | اعتبارسنجی `.env` و فایل‌های کانفیگ |
| `npm run sources:sync` | همگام‌سازی `config/sources.yaml` با دیتابیس |
| `npm run sources:list` | نمایش منابع و وضعیت سلامت هرکدام |
| `npm run kako -- sources:discover <نشانی>` | کشف خودکار فید RSS یک سایت |
| `npm run kako -- sources:test <slug>` | آزمودن یک منبع بدون ثبت در دیتابیس |
| `npm run collect` | اجرای یک دور جمع‌آوری |
| `npm run collect -- --source=<slug>` | جمع‌آوری فقط از یک منبع |
| `npm run collect -- --force` | جمع‌آوری فوری، بدون توجه به بازهٔ زمانی |
| `npm run filter` | فیلتر مرتبط‌بودن خبرها با شیراز |
| `npm run filter -- --dry-run` | نمایش تصمیم‌ها بدون تغییر در دیتابیس |
| `npm run dedup` | تشخیص خبرهای تکراری |
| `npm run rewrite` | بازنویسی خبرهای آماده و قرار دادن در صف تأیید |
| `npm run queue` | نمایش صف تأیید در خط فرمان |
| `npm run publish` | ارسال خبرهای تأییدشده به سایت و کانال تلگرام |
| `npm run publish -- --check` | آزمودن اتصال وردپرس و تلگرام بدون انتشار |
| `npm run publish -- --target=telegram` | فقط یک مقصد |
| `npm run publish -- --dry-run` | نمایش آنچه منتشر می‌شود، بدون ارسال |
| `npm run serve` | راه‌اندازی پنل مدیریت |
| `npm run admin:create -- --user=<نام> --password=<رمز>` | ساخت کاربر پنل یا تغییر رمز |
| `npm run pipeline -- --force` | اجرای کامل: جمع‌آوری ← فیلتر ← تکراری ← بازنویسی |
| `npm run worker` | اجرای مداوم کل پایپ‌لاین (`Ctrl+C` برای توقف) |
| `npm run doctor` | بررسی سلامت کل سامانه |
| `npm run doctor -- --deep` | همان، به‌همراه آزمودن اتصال وردپرس و تلگرام |
| `npm run cleanup -- --days=90` | پاک‌سازی دادهٔ قدیمی |
| `npm test` | اجرای تست‌ها |
| `npm run typecheck` | بررسی تایپ‌های TypeScript |
| `npm run build` | ساخت خروجی `dist/` (برای اجرا روی سرور) |

---

## افزودن منبع خبری جدید

منابع در فایل `config/sources.yaml` تعریف می‌شوند — **هیچ منبعی در کد نیست.**
راهنمای کامل هر گزینه با مثال، در `config/sources.example.yaml` آمده است.

### اگر منبع RSS دارد (حالت ترجیحی)

```yaml
  - slug: farsnews-shiraz          # شناسهٔ یکتا؛ فقط حروف کوچک انگلیسی و خط تیره
    name: "خبرگزاری فارس — فارس"
    type: rss
    url: "https://example.com/rss/fars"
    homepage: "https://example.com"
    enabled: true
    poll_interval_seconds: 900     # هر ۱۵ دقیقه
    fetch_full_content: true       # متن کامل را از صفحهٔ خود خبر بگیر
    article:
      body_selector: ".news-body"  # بدنهٔ متن در صفحهٔ خبر
      remove_selectors: [".related-news", ".ads"]
```

همیشه اگر سایت فید RSS دارد همین را انتخاب کنید: فید یک قرارداد پایدار است و
با عوض شدن قالب سایت خراب نمی‌شود.

### اگر منبع RSS ندارد

```yaml
  - slug: sitename-archive
    name: "نام سایت"
    type: scrape
    url: "https://example.com/archive/shiraz"   # صفحهٔ فهرست یا آرشیو
    enabled: true
    poll_interval_seconds: 1200
    list:                          # سلکتورهای صفحهٔ فهرست
      item_selector: "ul.news-list > li"
      title_selector: "h3 a"
      link_selector: "h3 a"
      summary_selector: "p.summary"
      date_selector: "time"
      date_attribute: "datetime"
    article:                       # سلکتورهای صفحهٔ اختصاصی خبر
      body_selector: "div.article-body"
      remove_selectors: [".related", ".tags"]
```

**پیدا کردن سلکتور:** صفحه را در مرورگر باز کنید ← روی عنوان خبر راست‌کلیک ←
Inspect ← روی المان راست‌کلیک ← Copy ← Copy selector. سلکتور را ساده کنید
(کلاس‌های تصادفی و شماره‌ها را بردارید). هرچه سلکتور ساده‌تر باشد، با تغییر
قالب سایت دیرتر خراب می‌شود.

### دو ابزار که کار افزودن منبع را ساده می‌کنند

**۱. پیدا کردن فید سایت**

```bash
npm run kako -- sources:discover https://www.isna.ir
```

فیدهای سایت را پیدا و آزمایش می‌کند و برای هرکدام می‌گوید چند خبر دارد،
متن کامل می‌دهد یا فقط خلاصه، و — مهم‌تر از همه — **چند درصد خبرهایش به
شیراز مربوط است**. در پایان یک بلوک آمادهٔ کپی برای `sources.yaml` می‌دهد.

```
✓ https://example.ir/rss
    «خبرگزاری آزمایشی — سراسری»
    مرتبط با شیراز: 0 از 3 نمونه          ← این فید به درد نمی‌خورد

✓ https://example.ir/rss/fars
    «خبرگزاری آزمایشی — استان فارس»
    مرتبط با شیراز: 5 از 5 نمونه          ← این را پیشنهاد می‌دهد
      ✓ [   12] بهره‌برداری از فاز نخست خط دو قطار شهری شیراز
      ~ [    2] برداشت انار از باغ‌های مرودشت آغاز شد
```

**۲. آزمودن منبع و تنظیم سلکتورها**

```bash
npm run kako -- sources:test isna-fars
```

دقیقاً همان کاری را می‌کند که جمع‌آوری واقعی می‌کند، ولی چیزی در دیتابیس
ثبت نمی‌کند. نشان می‌دهد از هر خبر **چه چیزی استخراج شده** و کدام سلکتور
کار نکرده است:

```
✓ 1. بهره‌برداری از فاز نخست خط دو قطار شهری شیراز
      امتیاز شیراز: 14
      تاریخ: ۷ شهریور ۱۴۰۵، ۰:۲۶
      متن  : 93 کلمه — «مدیرعامل سازمان قطار شهری شیراز و حومه…»

بررسی سلکتورها:
  ✗ متن خبر    .WRONG-SELECTOR
  ✓ تصویر      meta[property='og:image']
               → «https://example.ir/img/f1.jpg»

  ⚠️ 1 سلکتور چیزی نگرفت. خبرها با حدس‌های عمومی استخراج شدند —
     کار می‌کند، ولی با تغییر قالب سایت شکننده است.
```

این حلقه (سلکتور را عوض کن ← دستور را بزن ← ببین چه گرفت) تنها راه عملی
تنظیم سلکتور است.

### پس از هر ویرایش

```bash
npm run sources:sync                      # اعمال تغییرات فایل در دیتابیس
npm run kako -- sources:test <slug>       # آزمایش بدون ثبت در دیتابیس
npm run collect -- --source=<slug> --force  # جمع‌آوری واقعی
npm run sources:list                      # دیدن وضعیت سلامت منابع
```

### افزودن خبرگزاری‌های سراسری (ایسنا، فارس، مهر و…)

این خبرگزاری‌ها سراسری‌اند و فید اصلی‌شان پر از خبر تهران و اقتصاد کلان
است. **حتماً دنبال فید استان فارس بگردید**، وگرنه فیلتر شیراز مجبور
می‌شود صدها خبر بی‌ربط را کنار بگذارد و بی‌جهت به OpenAI هزینه می‌دهید.

```bash
npm run kako -- sources:discover https://www.isna.ir
npm run kako -- sources:discover https://farsnews.ir
npm run kako -- sources:discover https://www.mehrnews.com
```

الگوی آمادهٔ هر سه در `config/sources.example.yaml` هست.

> منبعی که از فایل حذف کنید **پاک نمی‌شود**، فقط غیرفعال می‌شود — چون خبرهای
> جمع‌آوری‌شده‌اش به آن ارجاع دارند و تاریخچه نباید از بین برود.

---

## آزمایش بدون منبع واقعی

منبع `mock-local` داده‌اش را از `fixtures/mock-source.json` می‌خواند تا بتوانید
کل پایپ‌لاین را بدون اینترنت و بدون فشار آوردن به سایت‌های واقعی اجرا کنید:

```bash
npm run sources:sync
npm run collect -- --force
```

دادهٔ نمونه عمداً ترکیبی است از چند خبر مرتبط با شیراز، یک خبر سراسری نامرتبط،
و یک خبر تکراری از زاویهٔ دیگر — تا در مایل‌استون‌های بعدی، فیلتر شیراز و
تشخیص تکراری روی دادهٔ واقع‌نما آزمایش شوند.

---

## فیلتر مرتبط‌بودن با شیراز

هر خبر خام پیش از بازنویسی باید ثابت کند که واقعاً خبر شیراز است. تصمیم در
دو لایه گرفته می‌شود تا هزینهٔ مدل زبانی فقط جایی خرج شود که لازم است:

```
امتیاز کلیدواژه‌ای ≥ ۴   → قطعاً مرتبط    (بدون تماس با مدل، رایگان)
امتیاز < ۱               → قطعاً نامرتبط  (بدون تماس با مدل، رایگان)
بین ۱ تا ۴               → از مدل زبانی پرسیده می‌شود
```

آستانه‌ها با آزمایش روی دادهٔ واقع‌نما تنظیم شده‌اند: خبر آشکارا شیرازی
معمولاً ۹ تا ۱۵ می‌گیرد، خبر سراسری منفی یا صفر.

### واژه‌نامه — `config/shiraz-keywords.yaml`

بیش از ۱۷۰ عبارت در گروه‌های وزن‌دار: نام شهر و استان، نهادهای شهری و
استانی، دانشگاه‌ها و بیمارستان‌ها، جاذبه‌ها و بناهای تاریخی، محله‌ها و
معابر، تیم‌های ورزشی، و شهرهای دیگر استان فارس (با وزن کمتر).

هرچه این فایل کامل‌تر باشد، فیلتر دقیق‌تر و تماس با مدل زبانی کمتر می‌شود.
**بدون نیاز به تغییر کد قابل ویرایش است.**

### تله‌هایی که فیلتر می‌شناسد

کلمهٔ «فارس» همیشه به استان فارس اشاره نمی‌کند. این عبارت‌ها پیش از
امتیازدهی از متن حذف می‌شوند:

| عبارت | چرا |
|---|---|
| خبرگزاری فارس | یک خبرگزاری سراسری است؛ بدون این، هر خبر فارس‌نیوز «محلی» شمرده می‌شد |
| خلیج فارس | جغرافیای ملی است، نه استان |
| زبان/ادبیات فارسی | ربطی به استان ندارد |

همچنین «فارسی» به‌اشتباه با «فارس» تطبیق نمی‌کند، ولی «شیرازی» و
«شیرازی‌ها» درست شناسایی می‌شوند.

### نشانه‌های منفی

نام شهرهای دیگر و نهادهای سراسری امتیاز را کم می‌کنند، اما **جریمه سقف
دارد** — تا خبری مثل «شهردار شیراز در تهران با وزیر کشور دیدار کرد»
به‌اشتباه کنار گذاشته نشود.

### وقتی مدل زبانی در دسترس نیست

اگر کلید OpenAI تنظیم نشده باشد یا تماس خطا بدهد، سیستم **متوقف نمی‌شود**:
تصمیم به معیار کلیدواژه‌ای برمی‌گردد و روش تصمیم `llm_failed` ثبت می‌شود تا
بعداً قابل بازبینی باشد.

### خبرهای نامرتبط پاک نمی‌شوند

با وضعیت `irrelevant` بایگانی می‌شوند، همراه با امتیاز، کلیدواژه‌های
پیداشده و دلیل تصمیم. این‌طور می‌توانید ببینید فیلتر کجا اشتباه کرده و
واژه‌نامه را بهتر کنید.

---

## تشخیص خبر تکراری

وقتی چند خبرگزاری یک رویداد را پوشش می‌دهند، کاکو نیوز نباید چند بار همان
خبر را منتشر کند. مشکل اینجاست که نگارش‌ها کاملاً متفاوت‌اند:

```
«بهره‌برداری از فاز نخست خط دو قطار شهری شیراز تا پایان سال»
«فاز اول خط ۲ مترو شیراز تا پایان سال افتتاح می‌شود»
```

هیچ هش یا تطبیق دقیقی این دو را یکی نمی‌بیند.

### روش سنجش

واژه‌های معنادار دو خبر با **ضریب هم‌پوشانی وزن‌دار** مقایسه می‌شوند:

- **چرا Jaccard نه:** Jaccard اختلاف طول را جریمه می‌کند؛ وقتی یک منبع خبر
  مفصل و دیگری خبر کوتاه می‌نویسد، امتیاز بی‌دلیل پایین می‌آید. ضریب
  هم‌پوشانی (تقسیم بر مجموعهٔ کوچک‌تر) دقیقاً برای همین حالت است.
- **عددها وزن دوبرابر دارند:** «۸۵ درصد» و «۴۰ هزار نفر» نشانهٔ قوی‌تری از
  هم‌رویداد بودن‌اند تا واژه‌های عمومی.
- امتیاز نهایی = نیمی از شباهت تیتر + نیمی از شباهت متن.

روی دادهٔ نمونه، جفت واقعاً تکراری **۰٫۶۵۵** گرفت و بیشترین امتیاز میان
خبرهای بی‌ربط **۰٫۱۴۴** بود — آستانهٔ ۰٫۴۵ در فاصلهٔ امنی میان این دو است.

### قدیمی‌ترین گزارش، خبر اصلی می‌شود

خبرها به ترتیب زمان انتشار پردازش می‌شوند و هر خبر فقط با خبرهایی مقایسه
می‌شود که **قبلاً از این مرحله رد شده‌اند**. نتیجه: همیشه اولین گزارش از یک
رویداد «خبر اصلی» است و بقیه به آن وصل می‌شوند؛ دو خبر هم‌زمان نمی‌توانند
یکدیگر را تکراری اعلام کنند.

### منبع دوم دور ریخته نمی‌شود

با `on_duplicate: "link"` در `app.yaml`، خبر تکراری به‌عنوان **منبع تکمیلی**
به خبر اصلی وصل می‌شود تا در بازنویسی بتوان از جزئیات هر دو استفاده کرد و
در پایان خبر همهٔ منابع ذکر شوند. اگر `ignore` بگذارید، فقط کنار گذاشته
می‌شود.

---

## موتور بازنویسی

خبر منبع هرگز عیناً منتشر نمی‌شود. مدل زبانی آن را از نو می‌نویسد تا به
خبری اصیل و متعلق به کاکو نیوز تبدیل شود.

### دو فایلی که سبک نگارش را تعیین می‌کنند

هر دو **بدون تغییر کد** قابل ویرایش‌اند و در بازنویسی بعدی اعمال می‌شوند:

| فایل | کار |
|---|---|
| `config/style-guide.md` | راهنمای سبک نگارشی: تیتر، لید، هرم وارونه، لحن، نگارش فارسی، قاعدهٔ «بازنویسی نه کپی» |
| `config/prompts/rewrite.system.md` | پرامپت سیستمی؛ راهنمای سبک و دسته‌بندی‌ها داخلش تزریق می‌شوند |

راهنمای سبک شامل نکته‌های عملی است: نیم‌فاصله، «ی» و «ک» فارسی، ارقام
فارسی، پرهیز از صفت‌های احساسی، فعل خنثی برای نقل قول، و اینکه هیچ عددی
حدس زده یا گرد نشود.

### خروجی ساختاریافته

مدل باید JSON با این ساختار برگرداند:

```json
{
  "title":    "تیتر خبری کوتاه",
  "lead":     "پاراگراف اول بر اساس هرم وارونه",
  "body":     "متن کامل با پاراگراف‌های جدا",
  "category": "یکی از دسته‌بندی‌های مجاز",
  "tags":     ["برچسب یک", "برچسب دو"]
}
```

### سه محافظ روی خروجی مدل

**۱. اعتبارسنجی و اصلاح ساختار** — مدل زبانی همیشه دقیقاً قالب خواسته‌شده
را نمی‌دهد. این‌ها خودکار اصلاح می‌شوند:

| مشکل | اصلاح |
|---|---|
| دسته‌بندی خارج از فهرست («شهری» به‌جای «شهری و عمرانی») | به نزدیک‌ترین دستهٔ مجاز نگاشت می‌شود |
| تکرار لید در ابتدای متن | پاراگراف تکراری حذف می‌شود |
| نوشتن خط «منبع: …» توسط مدل | حذف می‌شود؛ سامانه خودش می‌سازد |
| نشانه‌گذاری مارک‌داون در متن | پاک می‌شود |
| تگ تکراری، خالی، یا رشتهٔ جدا شده با ویرگول | مرتب و یکتا می‌شود |
| متن بیش از حد کوتاه | خبر رد می‌شود |

هر اصلاح در `rewrite_meta` ثبت می‌شود تا بشود کیفیت مدل را بازبینی کرد.

**۲. محافظ کپی عینی** — مهم‌ترین محافظ. مدل گاهی به‌جای بازنویسی، جمله‌های
منبع را با جابه‌جایی چند کلمه برمی‌گرداند. سامانه دنباله‌های ۸ کلمه‌ای متن
منبع را با متن بازنویسی‌شده مقایسه می‌کند:

| حالت | نسبت کپی | طولانی‌ترین دنبالهٔ مشترک |
|---|---|---|
| کپی کامل | ۱٫۰ | ۴۶ کلمه |
| تغییر چند کلمه (حالت خطرناک) | ۰٫۳۸۵ | ۱۷ کلمه |
| بازنویسی واقعی | ۰٫۰ | ۴ کلمه |

آستانه‌ها در `app.yaml`: `max_verbatim_ratio: 0.15` و `max_verbatim_run: 12`.

اگر خروجی رد شود، **یک بار دیگر با تذکر صریح‌تر** به مدل داده می‌شود. اگر
باز هم کپی بود، خبر با وضعیت `failed` کنار گذاشته می‌شود و **هرگز به صف
تأیید نمی‌رسد**.

> نقل قول مستقیم داخل گیومه از این سنجش کنار گذاشته می‌شود — حرف کسی را
> نباید عوض کرد.

**۳. ثبت هزینه** — تعداد توکن، مدت زمان و شمارهٔ تلاش هر تماس در
`rewrite_meta` ذخیره می‌شود.

### منابع تکمیلی

اگر خبری در مرحلهٔ تشخیص تکراری، «منبع تکمیلی» گرفته باشد، متن آن منابع هم
به مدل داده می‌شود تا جزئیاتی که فقط در آن‌ها آمده از دست نرود. خط پایانی
خبر هم نام همهٔ منابع را می‌آورد.

### خط «منبع: …» را سامانه می‌سازد، نه مدل

مدل صریحاً منع شده که این خط را بنویسد. سامانه آن را از روی **منابع واقعی
ثبت‌شده در دیتابیس** می‌سازد. دلیلش: نام منبع همیشه دقیق است، منابع تکمیلی
هم ذکر می‌شوند، و مدل نمی‌تواند نام منبعی را از خودش بسازد. قالبش در
`app.yaml` قابل تغییر است.

### نشانی سئوفرندلی فارسی

از تیتر، اسلاگ فارسی ساخته می‌شود:

```
«خط دو قطار شهری شیراز تا پایان سال به بهره‌برداری می‌رسد»
  ↓
/خط-دو-قطار-شهری-شیراز-تا-پایان-سال-به-بهره-برداری-می-رسد
```

اگر اسلاگ تکراری باشد پسوند عددی می‌گیرد تا نشانی خبر قدیمی نشکند.

### هیچ خبری خودکار منتشر نمی‌شود

خبر بازنویسی‌شده **همیشه** با وضعیت `pending_review` ثبت می‌شود. در کد هیچ
مسیری وجود ندارد که خبری را بدون عبور از صف تأیید منتشر کند.

```bash
npm run rewrite    # بازنویسی خبرهای آماده
npm run queue      # دیدن صف تأیید
```

---

## پنل مدیریت و صف تأیید

```bash
npm run serve
```

سپس در مرورگر: **http://127.0.0.1:7799**

کاربر اول در اولین اجرا از روی `ADMIN_USERNAME` و `ADMIN_PASSWORD` در `.env`
ساخته می‌شود. برای ساخت کاربر دیگر یا تغییر رمز:

```bash
npm run admin:create -- --user=sardabir --password=<رمز> --name="نام سردبیر"
```

### صفحه‌های پنل

| صفحه | کار |
|---|---|
| **صف تأیید** | آمار روز + فهرست خبرهای منتظر تصمیم |
| **بازبینی خبر** | متن منبع کنار متن بازنویسی‌شده، با امکان ویرایش |
| **منتشرشده** | خبرهای تأییدشده و منتشرشده |
| **ردشده** | خبرهای رد شده به‌همراه دلیل رد |
| **سلامت منابع** | وضعیت هر منبع، آخرین خطا و شمار شکست‌های پیاپی |

### صفحهٔ بازبینی — قلب کار سردبیر

صفحه دو ستونه است:

```
┌──────────────────────────┬──────────────────────────┐
│  متن منبع                │  متن کاکو نیوز           │
│  (فقط خواندنی)           │  (قابل ویرایش)           │
│                          │                          │
│  · تیتر اصلی             │  · تیتر                  │
│  · متن کامل خبر          │  · لید                   │
│  · لینک به سایت منبع     │  · متن                   │
│  · تاریخ انتشار          │  · دسته‌بندی (فهرست)     │
│                          │  · برچسب‌ها              │
│  ── منابع تکمیلی ──      │  · تصویر شاخص            │
│  · گزارش منبع دوم        │  · یادداشت سردبیر        │
└──────────────────────────┴──────────────────────────┘
                    ┌──────────────────────────┐
                    │ ☑ انتشار در وب‌سایت      │
                    │ ☑ انتشار در کانال تلگرام │
                    │ [ تأیید و انتشار ]       │
                    │ [ رد کردن ]  دلیل: ____  │
                    └──────────────────────────┘
```

سردبیر می‌تواند پیش از تأیید هر چیزی را عوض کند. ویرایش که ذخیره شود، خبر
با برچسب «ویرایش‌شده» علامت می‌خورد تا بعداً بشود دید مدل چقدر نیاز به
دست‌کاری داشته و راهنمای سبک را بر همان اساس بهبود داد.

### انتخاب مقصد انتشار

هنگام تأیید، مقصدها جداگانه انتخاب می‌شوند: فقط وب‌سایت، فقط تلگرام، یا هر
دو. مقصدهای پیش‌فرض در `app.yaml` زیر `publishing.default_targets` تنظیم
می‌شوند.

تأیید، خبری را مستقیم منتشر نمی‌کند؛ برای هر مقصد یک ردیف با وضعیت
`pending` در جدول `publications` می‌سازد که ماژول‌های انتشار (مایل‌استون ۶
و ۷) آن را برمی‌دارند. کلید یکتای (خبر، مقصد) تضمین می‌کند حتی با دو بار
زدن دکمهٔ تأیید، خبر دو بار در کانال پست نشود.

### امنیت پنل

پنل روی محتوایی کار می‌کند که از سایت‌های بیرونی آمده، پس این محافظ‌ها
اختیاری نیستند:

| تهدید | محافظ |
|---|---|
| کد جاوااسکریپت داخل تیتر یا متن منبع | همهٔ محتوا پیش از نمایش escape می‌شود؛ در پنل هیچ اسکریپتی اجرا نمی‌شود |
| سایت دیگری که مرورگر سردبیر را وادار به تأیید خبر کند | توکن CSRF وابسته به نشست روی همهٔ فرم‌ها |
| دزدیدن کوکی با جاوااسکریپت | کوکی `httpOnly` و `sameSite=strict` |
| دستکاری کوکی برای جا زدن به‌عنوان کاربر دیگر | کوکی با HMAC-SHA256 امضا شده است |
| حدس زدن رمز | scrypt با نمک تصادفی + کند شدن پس از ۸ تلاش ناموفق |
| افشای اینکه کدام نام کاربری وجود دارد | پیام خطا و زمان پاسخ برای «کاربر نیست» و «رمز غلط» یکسان است |
| نشانی `javascript:` در تصویر خبر | فقط `http` و `https` پذیرفته می‌شوند |

سیاست امنیتی محتوا (CSP) روی همهٔ صفحه‌ها اجرای هر اسکریپتی را ممنوع
می‌کند. به همین دلیل پنل **هیچ جاوااسکریپتی ندارد** — همه‌چیز با فرم‌های
ساده کار می‌کند. همچنین هیچ فونت یا استایلی از اینترنت بارگذاری نمی‌شود، تا
پنل روی سروری با دسترسی محدود هم درست کار کند.

> پنل را پشت HTTPS اجرا کنید. مقدار پیش‌فرض `ADMIN_HOST` برابر `127.0.0.1`
> است، یعنی فقط از خود سرور در دسترس است؛ برای دسترسی از بیرون آن را پشت
> یک reverse proxy (مثل Nginx) با گواهی SSL بگذارید.

---

## انتشار در سایت وردپرسی

ورودی این ماژول فقط خبرهایی است که **سردبیر تأیید کرده** — ردیف‌های
`publications` با مقصد `website` و وضعیت `pending`. هیچ مسیری وجود ندارد
که خبری بدون آن ردیف منتشر شود.

### آماده‌سازی وردپرس

۱. در پیشخوان وردپرس یک کاربر با نقش **Editor** یا **Author** بسازید
   (نقش Administrator لازم نیست و بهتر است داده نشود).

۲. برای همان کاربر «رمز برنامه» بسازید:
   **کاربران ← پروفایل ← Application Passwords ← افزودن**
   نامی مثل `kako-bot` بدهید. رمز تولیدشده فاصله دارد؛ **همان‌طور که هست**
   در `.env` بگذارید.

۳. مقادیر را در `.env` تنظیم کنید:

```bash
WORDPRESS_URL=https://kakonews.ir
WORDPRESS_USERNAME=kako-bot
WORDPRESS_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx
```

۴. اتصال را بیازمایید:

```bash
npm run publish -- --check
```

> اگر خطای «احراز هویت ناموفق» گرفتید: رمز اصلی کاربر را نگذاشته‌اید،
> بلکه باید **رمز برنامه** باشد. اگر REST API سایت‌تان بسته است، افزونهٔ
> امنیتی را بررسی کنید.

### آنچه در وردپرس ساخته می‌شود

| بخش خبر | در وردپرس |
|---|---|
| تیتر | عنوان پست |
| لید | پاراگراف اول پررنگ + خلاصهٔ پست (excerpt) |
| متن | پاراگراف‌های `<p>` |
| خط منبع | `<p class="kako-source">` در انتهای پست |
| دسته‌بندی | دستهٔ وردپرس (اگر نبود ساخته می‌شود) |
| برچسب‌ها | برچسب‌های وردپرس (اگر نبودند ساخته می‌شوند) |
| تصویر شاخص | آپلود در کتابخانهٔ رسانه و اتصال به پست |
| اسلاگ فارسی | نشانی پست |

ارقام انگلیسی متن هنگام انتشار به **فارسی** تبدیل می‌شوند (۸۵ به‌جای 85).

کلاس‌های `kako-lead`، `kako-source` و `kako-image-credit` روی پاراگراف‌ها
گذاشته می‌شوند تا در قالب سایت بتوانید جداگانه استایل بدهید.

### نگاشت دسته‌بندی

سامانه دستهٔ وردپرسی هم‌نام را پیدا می‌کند و اگر نبود می‌سازد. اگر
می‌خواهید به دستهٔ مشخصی وصل شود، شناسه‌اش را در `app.yaml` بدهید:

```yaml
publishing:
  wordpress:
    post_status: "publish"      # یا draft برای بازبینی دوباره در وردپرس
    category_map:
      "شهری و عمرانی": 12       # شناسهٔ دستهٔ موجود در وردپرس
      "گردشگری": 8
```

> **نکتهٔ ریز ولی مهم:** جست‌وجوی دسته در وردپرس تطبیق *جزئی* برمی‌گرداند.
> اگر سایت شما دستهٔ «ورزشی بانوان» داشته باشد و ما دنبال «ورزشی» بگردیم،
> وردپرس آن را هم برمی‌گرداند. سامانه نتیجه را خودش دقیق فیلتر می‌کند تا
> خبر به دستهٔ اشتباه نرود.

### وقتی چیزی خطا می‌دهد

| اتفاق | رفتار سامانه |
|---|---|
| سایت موقتاً پایین است (۵۰۳) | تلاش مجدد با فاصلهٔ فزاینده؛ اگر باز هم نشد، خبر در صف می‌ماند و اجرای بعدی دوباره تلاش می‌کند |
| رمز برنامه اشتباه است | خطای دائمی؛ بی‌جهت تکرار نمی‌شود و پیام راهنمای رفع می‌دهد |
| آپلود تصویر شکست خورد | خبر **بدون تصویر** منتشر می‌شود؛ تصویر مانع انتشار خبر نمی‌شود |
| ساخت یک برچسب شکست خورد | خبر با بقیهٔ برچسب‌ها منتشر می‌شود |
| خبر تأییدنشده به صف رسیده | انتشار انجام نمی‌شود و در پنل «ناموفق» ثبت می‌شود |

خطای انتشار خبر را از بین نمی‌برد: وضعیتش به «تأییدشده» برمی‌گردد و در
اجرای بعدی دوباره تلاش می‌شود.

### چه زمانی خبر «منتشرشده» می‌شود

اگر سردبیر هر دو مقصد را انتخاب کرده باشد، خبر تا وقتی **هر دو** ارسال
نشده‌اند در وضعیت `approved` می‌ماند. تنها پس از رفتن آخرین مقصد، وضعیتش
`published` می‌شود.

### API خواندنی

با اینکه سایت وردپرسی است، یک API خواندنی هم روی همان سرویس هست تا اگر
روزی خواستید فرانت‌اند اختصاصی بسازید مسیر باز بماند. فقط خبرهای
**منتشرشده** را می‌دهد:

| مسیر | خروجی |
|---|---|
| `GET /api/articles` | فهرست خبرها با صفحه‌بندی (`?page=`, `?per_page=`, `?category=`) |
| `GET /api/articles/<اسلاگ>` | یک خبر با متن کامل، خط منبع و فهرست منابع |
| `GET /api/categories` | دسته‌بندی‌ها با شمار خبر |

خبر در انتظار تأیید یا ردشده هرگز از این مسیر بیرون نمی‌رود.

---

## انتشار در کانال تلگرام

### آماده‌سازی ربات

۱. در تلگرام به [@BotFather](https://t.me/BotFather) پیام بدهید و با
   `/newbot` یک ربات بسازید. توکنی که می‌دهد را در `.env` بگذارید.

۲. **ربات را در کانال ادمین کنید** — این مرحله را فراموش نکنید. بدون آن
   تلگرام خطای «bot is not a member of the channel» می‌دهد.
   کانال ← مدیریت ← مدیران ← افزودن مدیر ← ربات را انتخاب کنید و
   دسترسی «ارسال پیام» را بدهید.

۳. شناسهٔ کانال را تنظیم کنید:

```bash
TELEGRAM_BOT_TOKEN=123456789:AA...
TELEGRAM_CHANNEL_ID=@kakonews        # یا شناسهٔ عددی مثل -1001234567890
```

۴. اتصال را بیازمایید:

```bash
npm run publish -- --check
```

### شکل پست در کانال

```
┌────────────────────────────────────────────┐
│  [ عکس شاخص خبر ]                          │
│                                            │
│  خط دو قطار شهری شیراز تا پایان سال       │  ← تیتر، بولد
│  به بهره‌برداری می‌رسد                     │
│                                            │
│  چهار ایستگاه نخست خط دو مترو شیراز تا     │  ← لید
│  پایان امسال آمادهٔ پذیرش مسافر می‌شود.    │
│                                            │
│  پیشرفت فیزیکی این بخش از مرز ۸۵ درصد     │  ← متن
│  گذشته است.                                │
│                                            │
│  منبع: خبرگزاری نمونه                      │
│  ادامه در سایت کاکو نیوز                   │  ← لینک به خبر در سایت
│  کاکو نیوز | اخبار شیراز                   │  ← امضای کانال
└────────────────────────────────────────────┘
```

اگر خبر تصویر داشته باشد، به‌صورت **عکس + کپشن** می‌رود، نه متن خالی.

### محدودیت ۱۰۲۴ نویسه — و اینکه چرا مهم است

کپشن عکس در تلگرام حداکثر **۱۰۲۴ نویسه** است (پیام متنی ۴۰۹۶). یک خبر
معمولی راحت از این رد می‌شود. اگر کورکورانه ببریم، اولین چیزی که قربانی
می‌شود لینک «ادامه در سایت» است — یعنی دقیقاً همان چیزی که پست برای آن
فرستاده می‌شود.

پس ترتیب اولویت **از آخر به اول** است:

| بخش | سرنوشت |
|---|---|
| تیتر | همیشه می‌ماند |
| لینک «ادامه در سایت» | همیشه می‌ماند |
| خط منبع و امضای کانال | همیشه می‌مانند |
| **متن خبر** | تنها چیزی که کوتاه می‌شود، پاراگراف‌به‌پاراگراف |

در آزمایش، خبری با ۸٬۴۹۰ نویسه به پیامی ۴٬۰۴۳ نویسه‌ای تبدیل شد و تیتر و
لینک هر دو سر جایشان ماندند.

### نکته‌های فنی که رعایت شده‌اند

| موضوع | رفتار سامانه |
|---|---|
| نویسهٔ `<` در متن خبر | فرار داده می‌شود؛ بدون آن تلگرام کل پیام را با خطای «can't parse entities» رد می‌کند |
| محدودیت نرخ (۴۲۹) | تلگرام خودش می‌گوید چند ثانیه صبر کنیم؛ **دقیقاً همان‌قدر** صبر می‌شود (تلاش زودتر محدودیت را طولانی‌تر می‌کند) |
| فاصلهٔ بین پست‌ها | ۳ ثانیه پیش‌فرض، در `app.yaml` قابل تغییر |
| بریدن متن | روی مرز کلمه و بدون شکستن موجودیت HTML مثل `&amp;` |
| تصویر | فایلی که برای وردپرس دانلود شده دوباره استفاده می‌شود، نه دانلود دوباره |
| اگر ارسال عکس نشد | خبر به‌صورت **متن** فرستاده می‌شود؛ تصویر مانع انتشار خبر نمی‌شود |
| توکن غلط یا پیام نامعتبر | خطای دائمی، بدون تکرار بی‌جهت |

### چرا اول سایت، بعد تلگرام

فرمان `publish` همیشه اول سایت را منتشر می‌کند و بعد تلگرام. دلیلش این است
که لینک پست تلگرام به **نشانی واقعی خبر در وردپرس** اشاره کند، نه نشانی
حدسی. اگر خبر فقط برای تلگرام تأیید شده باشد، نشانی از روی اسلاگ ساخته
می‌شود.

---

## چرخهٔ کامل وضعیت یک خبر

```
                       ┌─────────────┐
   جمع‌آوری  ─────────▶ │     new     │
                       └──────┬──────┘
                              │  فیلتر شیراز
                    ┌─────────┴─────────┐
                    ▼                   ▼
            ┌──────────────┐     ┌─────────────┐
            │  irrelevant  │     │  new (امتیاز‌دار) │
            │  (بایگانی)   │     └──────┬──────┘
            └──────────────┘            │  تشخیص تکراری
                              ┌─────────┴─────────┐
                              ▼                   ▼
                      ┌──────────────┐     ┌─────────────┐
                      │  duplicate   │     │    ready    │
                      │ (منبع تکمیلی)│     │ آمادهٔ بازنویسی│
                      └──────┬───────┘     └──────┬──────┘
                             │  بازنویسی          │
                             └────────┬───────────┘
                                      ▼
                            ┌───────────────────┐
                            │  articles         │
                            │  pending_review   │  ← منتظر تصمیم سردبیر
                            └─────────┬─────────┘
                          ┌───────────┴───────────┐
                          ▼                       ▼
                  ┌───────────────┐       ┌──────────────┐
                  │   rejected    │       │   approved   │
                  │  (با دلیل رد) │       └──────┬───────┘
                  └───────────────┘              │
                                                 ▼
                                        ┌──────────────────┐
                                        │  publications    │
                                        │  وب‌سایت / تلگرام │
                                        └────────┬─────────┘
                                                 │ پس از ارسال همهٔ مقصدها
                                                 ▼
                                        ┌──────────────────┐
                                        │    published     │
                                        └──────────────────┘
```

هر گذار در جدول `pipeline_events` ثبت می‌شود، پس همیشه می‌توان دید یک خبر
دقیقاً کجا و چرا کنار گذاشته شده است.

---

## رفتار سیستم هنگام خطای منبع

| اتفاق | واکنش سیستم |
|---|---|
| سایت منبع پایین است | خطا در `sources.last_error` ثبت، منبع skip، **بقیهٔ منابع ادامه** |
| قالب سایت عوض شده (سلکتور بی‌نتیجه) | خطای گویا: «احتمالاً قالب سایت منبع تغییر کرده است» |
| صفحهٔ یک خبر باز نشد | همان خبر با اطلاعات فهرست ذخیره می‌شود، بقیه ادامه |
| یک آیتم فهرست ناقص است | فقط هشدار؛ آیتم رد می‌شود |
| پاسخ ۵۰۰ یا ۴۲۹ | تلاش مجدد با فاصلهٔ فزاینده (۱، ۲، ۴ ثانیه) |

شمار شکست‌های پیاپی هر منبع در `sources.consecutive_failures` نگه‌داری می‌شود
تا در پنل مدیریت منابع مشکل‌دار قابل تشخیص باشند.

---

## ساختار پوشه‌ها

```
kako-news/
├── config/
│   ├── app.yaml              ← تنظیمات عمومی (دسته‌بندی‌ها، آستانه‌ها، برند)
│   ├── sources.yaml          ← فهرست منابع خبری (این را ویرایش کنید)
│   ├── sources.example.yaml  ← نمونهٔ پرتوضیح منابع
│   ├── shiraz-keywords.yaml  ← واژه‌نامهٔ مرتبط‌بودن با شیراز
│   ├── style-guide.md        ← راهنمای سبک نگارشی (این را ویرایش کنید)
│   └── prompts/
│       ├── relevance.system.md  ← پرامپت طبقه‌بندی مرتبط‌بودن
│       └── rewrite.system.md    ← پرامپت بازنویسی خبر
├── fixtures/
│   └── mock-source.json      ← دادهٔ نمونه برای آزمایش پایپ‌لاین
├── migrations/
│   ├── 001_init.sql          ← اسکیمای دیتابیس
│   ├── 002_source_type_mock.sql
│   └── 003_filter_and_dedup.sql
├── src/
│   ├── config/               ← بارگذاری و اعتبارسنجی تنظیمات
│   │   ├── env.ts            ← متغیرهای محیطی (.env)
│   │   ├── app-config.ts     ← فایل app.yaml
│   │   ├── sources-config.ts ← فایل sources.yaml
│   │   ├── keywords-config.ts← واژه‌نامهٔ شیراز
│   │   └── paths.ts          ← مسیرهای پایهٔ پروژه
│   ├── db/
│   │   ├── pool.ts           ← استخر اتصال و کوئری/تراکنش
│   │   └── migrate.ts        ← اجراکنندهٔ مهاجرت‌ها
│   ├── lib/
│   │   ├── logger.ts         ← لاگ ساختاریافته بر اساس مرحلهٔ پایپ‌لاین
│   │   ├── errors.ts         ← مدیریت خطا؛ خطای یک منبع بقیه را متوقف نمی‌کند
│   │   ├── text.ts           ← نرمال‌سازی متن فارسی، هش محتوا، HTML به متن
│   │   ├── date.ts           ← تحلیل تاریخ میلادی و شمسی
│   │   ├── similarity.ts     ← سنجش شباهت دو خبر
│   │   ├── openai.ts         ← کلاینت OpenAI
│   │   └── http.ts           ← دریافت صفحه با مهلت، تلاش مجدد و تشخیص کدگذاری
│   ├── collectors/           ← آداپتورهای منابع خبری
│   │   ├── types.ts          ← قرارداد مشترک آداپتورها
│   │   ├── registry.ts       ← ثبت‌گاه آداپتورها
│   │   ├── rss.ts            ← آداپتور فید RSS/Atom
│   │   ├── scrape.ts         ← آداپتور سایت‌های بدون فید
│   │   ├── mock.ts           ← آداپتور دادهٔ تستی محلی
│   │   └── article-page.ts   ← استخراج متن کامل از صفحهٔ خبر
│   ├── pipeline/
│   │   ├── collect.ts        ← هماهنگ‌کنندهٔ جمع‌آوری
│   │   ├── scheduler.ts      ← زمان‌بند اجرای دوره‌ای
│   │   ├── relevance-score.ts← امتیازدهی کلیدواژه‌ای شیراز
│   │   ├── relevance.ts      ← تصمیم نهایی مرتبط‌بودن
│   │   ├── dedup.ts          ← تشخیص خبر تکراری
│   │   ├── rewrite.ts        ← موتور بازنویسی
│   │   └── rewrite-validate.ts ← اعتبارسنجی خروجی مدل
│   ├── admin/                ← پنل تأیید
│   │   ├── server.ts         ← مسیرها، احراز هویت، محافظ CSRF
│   │   ├── auth.ts           ← هش رمز با scrypt، نشست، توکن CSRF
│   │   ├── html.ts           ← ساخت HTML امن (escape خودکار)
│   │   ├── api.ts            ← API خواندنی خبرهای منتشرشده
│   │   └── views/            ← پوسته و صفحه‌ها (راست‌چین فارسی)
│   ├── publisher/            ← انتشار
│   │   ├── wordpress.ts      ← کلاینت WordPress REST API
│   │   ├── website.ts        ← هماهنگ‌کنندهٔ انتشار در سایت
│   │   ├── telegram.ts       ← کلاینت Telegram Bot API
│   │   ├── channel.ts        ← هماهنگ‌کنندهٔ انتشار در کانال
│   │   ├── render.ts         ← تبدیل خبر به قالب هر مقصد
│   │   └── images.ts         ← دانلود تصویر شاخص (با هشدار حق نشر)
│   ├── db/repositories/      ← کوئری‌های هر جدول (منابع، خبر خام، خبر، انتشار)
│   └── cli/
│       ├── index.ts          ← نقطهٔ ورود خط فرمان
│       ├── doctor.ts         ← بررسی سلامت سامانه
│       ├── discover.ts       ← کشف فید RSS سایت خبری
│       ├── test-source.ts    ← آزمودن منبع و تنظیم سلکتور
│       └── cleanup.ts        ← پاک‌سازی دادهٔ قدیمی
├── tests/                    ← تست‌ها (سرور HTTP محلی، بدون نیاز به اینترنت)
├── data/                     ← فایل‌های محلی (تصاویر دانلودشده)
├── deploy/                   ← فایل‌های راه‌اندازی روی سرور
│   ├── install.sh            ← اسکریپت نصب
│   ├── kako-worker.service   ← سرویس پس‌زمینه
│   ├── kako-panel.service    ← سرویس پنل
│   ├── kako-backup.{service,timer}   ← پشتیبان‌گیری روزانه
│   ├── kako-cleanup.{service,timer}  ← پاک‌سازی هفتگی
│   ├── backup.sh / restore.sh
│   └── nginx-kako.conf       ← پروکسی با SSL
├── docker-compose.yml        ← دیتابیس محلی برای توسعه
└── .env.example              ← نمونهٔ فایل تنظیمات محیطی
```

---

## جدول‌های دیتابیس

| جدول | کار |
|---|---|
| `sources` | منابع خبری (آینهٔ `config/sources.yaml` + وضعیت اجرایی هر منبع) |
| `raw_articles` | خبر خام پیش از هر پردازشی، همراه با هش محتوا برای تشخیص تکراری |
| `articles` | خبر بازنویسی‌شدهٔ کاکو نیوز با وضعیت صف تأیید |
| `article_sources` | پیوند خبر به منبع اصلی و منابع تکمیلی |
| `publications` | نتیجهٔ انتشار در هر مقصد (سایت / تلگرام) |
| `job_runs` | تاریخچهٔ اجرای کارهای دوره‌ای |
| `pipeline_events` | ردپای هر خبر در تمام مراحل، برای دیباگ |
| `admin_users` | کاربران پنل تأیید (رمز با scrypt هش می‌شود) |

### وضعیت‌های خبر

**`raw_articles.status`**
`new` → تازه جمع‌آوری شده · `irrelevant` → مرتبط با شیراز نبود ·
`duplicate` → تکراری بود · `ready` → آمادهٔ بازنویسی ·
`processing` → در حال بازنویسی · `processed` → بازنویسی شد · `failed` → خطا خورد

**`articles.status`**
`pending_review` → منتظر تأیید سردبیر · `approved` → تأیید شد و در صف انتشار ·
`publishing` → در حال ارسال · `published` → منتشر شد ·
`rejected` → رد شد · `failed` → انتشار خطا خورد

**`publications.status`**
`pending` → در صف ارسال · `sent` → ارسال شد ·
`failed` → ارسال خطا خورد · `skipped` → صرف‌نظر شد

---

## راه‌اندازی روی سرور

تا اینجا همه‌چیز دستی اجرا می‌شد. برای اینکه سامانه شبانه‌روز خودش کار
کند، دو سرویس روی سرور اجرا می‌شوند:

| سرویس | کار |
|---|---|
| `kako-worker` | جمع‌آوری، فیلتر، تشخیص تکراری، بازنویسی و انتشار — به‌صورت دوره‌ای |
| `kako-panel` | پنل تأیید که سردبیر با آن کار می‌کند |

به‌علاوهٔ دو زمان‌بند: پشتیبان‌گیری روزانه و پاک‌سازی هفتگی.

### نصب

```bash
sudo git clone <آدرس-مخزن> /opt/kako-news
cd /opt/kako-news
sudo ./deploy/install.sh
```

اسکریپت نصب کاربر سرویس، دیتابیس و فایل‌های systemd را آماده می‌کند و
مراحل باقی‌مانده را می‌گوید. سپس:

```bash
sudo cp .env.example .env && sudo nano .env    # مقادیر را پر کنید
sudo -u kako npm ci --omit=dev
sudo -u kako npm run migrate
sudo -u kako npm run sources:sync
sudo -u kako npm run doctor -- --deep          # بررسی سلامت
sudo systemctl enable --now kako-worker kako-panel
sudo systemctl enable --now kako-backup.timer kako-cleanup.timer
```

پنل روی `127.0.0.1:7799` گوش می‌دهد و **مستقیم از اینترنت در دسترس
نیست**. برای دسترسی از بیرون، Nginx را با گواهی SSL تنظیم کنید:

```bash
sudo cp deploy/nginx-kako.conf /etc/nginx/sites-available/kako-panel
sudo ln -s /etc/nginx/sites-available/kako-panel /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
sudo certbot --nginx -d panel.kakonews.ir
```

### زمان‌بندی خودکار

سرویس `kako-worker` هر ۳۰ ثانیه یک «تیک» می‌زند و کارهایی که وقتشان
رسیده را انجام می‌دهد. بازه‌ها در `app.yaml`:

```yaml
scheduler:
  pipeline_interval_seconds: 300   # فیلتر، تشخیص تکراری، بازنویسی
  publish_interval_seconds: 120    # ارسال خبرهای تأییدشده
  stages:
    collect: true
    filter: true
    dedup: true
    rewrite: true    # برای کنترل هزینهٔ OpenAI می‌توانید خاموشش کنید
    publish: true
```

بازهٔ خواندن **هر منبع** جداگانه در `sources.yaml` تعریف می‌شود.

> **صف تأیید عمداً وسط این زنجیره است.** زمان‌بند خبر را تا
> `pending_review` جلو می‌برد و آنجا می‌ایستد. مرحلهٔ انتشار فقط چیزهایی
> را برمی‌دارد که سردبیر در پنل تأیید کرده است.

### دیدن لاگ‌ها

```bash
journalctl -u kako-worker -f          # لاگ زندهٔ سرویس
journalctl -u kako-worker --since today | grep '"level":"error"'
systemctl status kako-worker kako-panel
```

روی سرور لاگ به‌صورت JSON نوشته می‌شود (متغیر `LOG_FORMAT=json` در فایل
سرویس) تا فیلدها قابل جست‌وجو باشند. در حالت توسعه، خروجی خوانا و رنگی است.

### بررسی سلامت

```bash
npm run doctor           # سریع
npm run doctor -- --deep # به‌همراه آزمودن اتصال وردپرس و تلگرام
```

`doctor` این‌ها را بررسی می‌کند: درستی فایل‌های تنظیمات، اتصال و
مهاجرت‌های دیتابیس، سلامت هر منبع خبری، تنظیم بودن سرویس‌های بیرونی،
انباشته شدن صف تأیید، انتشارهای منتظر تلاش مجدد، و خطاهای ۲۴ ساعت اخیر.
اگر مشکل جدی باشد، کد خروج غیرصفر می‌دهد — پس می‌شود در مانیتورینگ
استفاده‌اش کرد.

### پشتیبان‌گیری

```bash
./deploy/backup.sh                    # دستی
systemctl list-timers kako-backup     # زمان اجرای بعدی
```

هر پشتیبان شامل کل دیتابیس (خبرها، منابع، کاربران پنل، تاریخچه) و
فایل‌های `config/` است. پشتیبان‌های قدیمی‌تر از ۱۴ روز خودکار حذف
می‌شوند (`KAKO_BACKUP_KEEP_DAYS`).

> **فایل `.env` عمداً در پشتیبان نیست** — کلیدهای API دارد. آن را
> جداگانه و در جای امن نگه دارید.

بازگردانی:

```bash
./deploy/restore.sh backups/kako-db-YYYYMMDD-HHMMSS.dump
```

اسکریپت پیش از بازنویسی، تأیید می‌گیرد، سرویس‌ها را متوقف می‌کند،
بازمی‌گرداند، مهاجرت‌های جدید را اعمال می‌کند و دوباره سرویس‌ها را
بالا می‌آورد.

### پاک‌سازی دادهٔ قدیمی

```bash
npm run cleanup -- --days=90 --dry-run   # اول ببینید چه چیزی پاک می‌شود
npm run cleanup -- --days=90
```

محافظه‌کارانه است: خبرِ **منتشرشده** و هر خبری که به آن وصل است هرگز
پاک نمی‌شود، و خبر در انتظار تأیید هم — هرچقدر قدیمی باشد. فقط خبر خامِ
نامرتبط یا تکراری، رویدادهای قدیمی و تصاویر بی‌استفاده.

### وقتی انتشار خطا می‌دهد

خطای گذرا (قطعی سایت، محدودیت نرخ) خبر را کنار نمی‌گذارد؛ فقط تلاش بعدی
را عقب می‌برد:

```
تلاش ۱ ناموفق → ۵ دقیقه بعد
تلاش ۲ ناموفق → ۱۰ دقیقه بعد
تلاش ۳ ناموفق → ۲۰ دقیقه بعد
تلاش ۴ ناموفق → ۴۰ دقیقه بعد
تلاش ۵ ناموفق → «ناموفق» علامت می‌خورد و در پنل دیده می‌شود
```

خطای دائمی (رمز اشتباه، پیام نامعتبر) بی‌جهت تکرار نمی‌شود و همان بار
اول «ناموفق» می‌شود. در صفحهٔ هر خبر در پنل، دکمهٔ **«تلاش مجدد»** هست
تا سردبیر پس از رفع مشکل، دوباره به صف بفرستدش.

### فهرست بررسی پیش از راه‌اندازی نهایی

- [ ] `ADMIN_PASSWORD` از مقدار پیش‌فرض عوض شده باشد
- [ ] `SESSION_SECRET` یک رشتهٔ تصادفی ۶۴ نویسه‌ای باشد
- [ ] دسترسی فایل `.env` روی `600` و مالکش کاربر سرویس باشد
- [ ] پنل پشت HTTPS باشد و پورت ۷۷۹۹ از بیرون بسته باشد
- [ ] کاربر وردپرس نقش Editor داشته باشد، نه Administrator
- [ ] ربات تلگرام در کانال **ادمین** باشد
- [ ] `npm run doctor -- --deep` بدون مشکل جدی پاس شود
- [ ] یک بار پشتیبان بگیرید و بازگردانی‌اش را در یک دیتابیس آزمایشی امتحان کنید

---

## اصول ثابت پروژه

1. **هیچ کلیدی در کد نیست.** همهٔ کلیدها و رمزها از `.env` می‌آیند.
2. **هیچ چیزی هاردکد نیست.** منابع خبری، دسته‌بندی‌ها، آستانه‌ها و راهنمای
   نگارشی همه در فایل‌های `config/` قابل ویرایش‌اند.
3. **متن منبع هرگز عیناً کپی نمی‌شود.** هم به دلیل حق نشر، هم برای اصالت محتوای
   کاکو نیوز.
4. **هیچ خبری خودکار منتشر نمی‌شود.** هر خبر پیش از انتشار از صف تأیید رد می‌شود.
5. **خطای یک منبع، بقیه را زمین نمی‌زند.** منبع خطادار skip و لاگ می‌شود.
6. **هیچ داده‌ای پاک نمی‌شود.** خبرهای نامرتبط یا تکراری فقط وضعیت‌شان عوض
   می‌شود تا بعداً بتوان دلیل تصمیم را بررسی کرد.

---

## نقشهٔ راه

| مایل‌استون | عنوان | وضعیت |
|---|---|---|
| ۱ | اسکلت پروژه + تنظیمات + دیتابیس | ✅ انجام شد |
| ۲ | ماژول جمع‌آوری: آداپتور RSS و Scrape + زمان‌بند + منبع تستی | ✅ انجام شد |
| ۳ | فیلتر مرتبط‌بودن با شیراز + تشخیص تکراری | ✅ انجام شد |
| ۴ | موتور بازنویسی با OpenAI + راهنمای سبک نگارشی | ✅ انجام شد |
| ۵ | صف تأیید + پنل مدیریت | ✅ انجام شد |
| ۶ | انتشار در سایت وردپرسی | ✅ انجام شد |
| ۷ | انتشار در کانال تلگرام | ✅ انجام شد |
| ۸ | مدیریت تصاویر، تست‌ها و راه‌اندازی روی سرور | ✅ انجام شد |
